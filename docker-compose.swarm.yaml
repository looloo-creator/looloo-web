version: "3.0"
services:
  app:
    image: rajasekar9698/one-pay-admin:latest
    ports:
      - "4200:80"
    networks:
      - one_pay_swarm_network

networks:
  one_pay_swarm_network:
    external: true
